name: CI Pipeline

permissions:
  contents: read

on:
  push:
    branches: [develop]
  pull_request:
    branches: [develop]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout  # Rajouter SHA
      - name: Build Docker image
        run: docker build -t ai-prompt-lib .
      - run: echo "Je build"

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout  # Rajouter SHA
      - run: echo "Je teste"